# app — L1 (the verbs)

The universal verbs (orchestration over the ports), the `AssetcoreService` facade
that bundles a repo + sink, and the observability helpers behind `/metrics`.

## Verbs

::: assetcore.app.verbs

## Service facade

::: assetcore.app.services

## Observability

::: assetcore.app.observability
