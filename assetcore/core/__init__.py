"""L0 — the pure, tool-agnostic core domain.

Imports nothing but the Python standard library. No I/O, no frameworks, no tool
names. Everything here is plain data (`entities`), enums (`types`), interfaces
the outside world implements (`ports`), and pure business logic (`rules`).
"""
